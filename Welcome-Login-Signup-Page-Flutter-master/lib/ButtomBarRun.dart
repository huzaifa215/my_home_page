import 'package:flutter/material.dart';
import 'buttombar.dart';

class ButtomBarRun extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Buttom(),
    );
  }
}